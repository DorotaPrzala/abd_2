<p id="notice"><%= notice %></p>

<h1>Orders</h1>



<table>
  <tr>
    <th>ID</th>
    <th>Status</th>
    <th>Szczególy</th>
    <th></th>
  </tr>

  <% @orders.each do |order| %>
    <tr>
      <td><%= order.id %></td>
      <td><%= order.status %></td>
 <%#      <td><%= link_to 'Show/', orders_path(order)%></td>
      <td><%= link_to 'Details', order %></td>
    </tr>
  <% end %>
</table>

<br>



<%= form_with(model: product_to_order, local: true) do |form| %>
  <% if product_to_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_to_order.errors.count, "error") %> prohibited this product_to_order from being saved:</h2>

      <ul>
      <% product_to_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
